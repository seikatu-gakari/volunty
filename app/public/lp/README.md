# LP画像アセット

`volunty-match-preview.png` は未ログイントップのアプリUIプレビュー画像です。

- LP上のUI表現だけを更新したい場合は、同じファイル名で画像を差し替える。
- 推奨サイズは `1568 x 1003` 付近の横長PNG。
- 表示パス、alt、画像サイズは `app/src/app/components/lp/lpPreviewImage.ts` に集約する。
